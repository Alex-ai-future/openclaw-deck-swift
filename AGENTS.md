# AGENTS.md - 项目工作规则

**项目：** OpenClaw Deck Swift  
**位置：** `~/Projects/openclaw-deck-swift/`  
**使用说明书：** [docs/USER_GUIDE.md](docs/USER_GUIDE.md)

---

## 📚 项目文档

### 文档分工原则

**三个文档，明确分工，避免重复：**

| 文档 | 目标读者 | 内容重点 | 篇幅 | 写作原则 |
|------|---------|---------|------|---------|
| **README.md** | 访问者 | 项目简介 + 快速开始 + 文档索引 | ≤100 行 | 极简，只引用不展开 |
| **USER_GUIDE.md** | 用户 | 怎么使用 + 故障排除 | 按任务组织 | **50% 篇幅用于故障排除** |
| **introduction.md** | 开发者 | 技术架构 + 设计决策 + 开发指南 | ≤500 行 | 纯技术，不写功能说明 |

**USER_GUIDE.md 写作标准：**
- ✅ 按"我要做 X"组织，不是按"功能 Y"组织
- ✅ **故障排除占 50% 篇幅**（连接问题、发送失败、常见错误）
- ✅ 每个问题包含：症状、检查步骤、解决方法
- ❌ 不写技术实现细节
- ❌ 不写设计原理

**introduction.md 写作标准：**
- ✅ 架构设计和技术选型
- ✅ 解释"为什么这样设计"
- ✅ 开发指南和测试标准
- ❌ 不写功能说明（用户指南里有）
- ❌ 不写 API 细节（代码注释里有）

**README.md 写作标准：**
- ✅ 一句话说明项目
- ✅ 3 步快速开始
- ✅ 文档链接索引
- ❌ 不写功能列表
- ❌ 不写技术细节

**核心原则：**
1. **用户文档 = 故障排除手册** - 用户遇到问题时能快速找到答案
2. **技术文档 = 架构指南** - 新开发者能快速理解架构
3. **README = 门面** - 5 秒内理解项目是做什么的

---

---

## 🛑 核心规则：修改前必须确认

**在任何修改操作之前，必须遵循以下流程：**

```
1. 分析需求 → 2. 制定计划 → 3. 向用户确认 → 4. 获得批准 → 5. 执行修改
```

### 具体要求：

1. **不要直接修改文件**
   - 即使是很小的改动，也要先告知用户
   - 说明你要改什么、为什么改、怎么改

2. **提供完整的修改计划**
   - 列出所有要修改的文件
   - 说明每处修改的内容
   - 解释修改的原因和预期效果

3. **等待用户批准**
   - 用户说"可以"、"好的"、"批准"等明确表示后，才能执行
   - 如果用户有疑问，先解答再继续

4. **修改后汇报**
   - 完成后告知用户修改已完成
   - 如有需要，说明如何验证修改效果

5. **禁止直接修改代码** ⚠️
   - 任何代码修改前，必须先提供**逻辑严密的修改计划**
   - 计划必须包含：问题分析、修改方案、影响范围、预期效果
   - 没有计划的修改请求，先制定计划再执行
   - 这是保护项目的第一道防线

---

## 📋 工作流程

### 进入项目时：
- [ ] 读取本 `AGENTS.md` 文件
- [ ] 了解项目结构和当前状态
- [ ] 等待用户指令

### 收到任务时：
- [ ] 分析任务需求
- [ ] 制定详细计划
- [ ] **向用户确认计划** ⚠️
- [ ] 获得批准后执行
- [ ] 完成后汇报

### 日常操作：
- [ ] 读取文件、探索代码 → ✅ 可以直接做
- [ ] 修改文件、删除文件、创建文件 → ⚠️ **必须先确认**
- [ ] 运行构建/测试命令 → ⚠️ **必须先确认**
- [ ] 执行 git 操作（commit/push 等）→ ⚠️ **必须先确认**

---

## 💡 示例

### ❌ 错误做法
```
用户：帮我修复这个 bug
助手：好的，我来改...（直接修改文件）
```

### ✅ 正确做法
```
用户：帮我修复这个 bug
助手：好的，我分析一下...

**修改计划：**
1. 文件：`Views/DeckView.swift`
   - 问题：第 45 行空指针异常
   - 修改：添加可选绑定检查
2. 文件：`ViewModels/DeckViewModel.swift`
   - 问题：session 查找逻辑有误
   - 修改：改为大小写不敏感匹配

请确认这个计划是否可以执行？

用户：可以
助手：好的，开始执行修改...
```

---

## 📝 备注

- 这个规则是为了保护项目安全，避免意外修改
- 用户可能有你不知道的上下文或考虑
- 有疑问时，宁可多问一次，也不要擅自行动

---

## 📝 文档编写规则

### 文档内容规范

**✅ 应该写的内容：**
- 功能说明和使用方法
- 技术架构和实现细节
- 故障排除指南
- 最佳实践

**❌ 不应该写的内容：**
- **变更日志/更新记录** - 不要记录"某月某日添加了什么功能"
- 版本历史
- 临时性的开发笔记

**原因：**
- 文档应该聚焦在"如何使用"和"是什么"
- 变更记录应该通过 git history 查看
- 保持文档简洁，便于维护

### 示例

#### ❌ 错误做法
```markdown
## 更新记录
- 2026-02-26 添加了输入框自动增长功能
- 2026-02-25 修复了编译警告
```

#### ✅ 正确做法
```markdown
## 功能说明
### 输入框
输入框会根据文本内容自动调整高度，最小高度 36pt，最大高度 150pt。
```

---

## 🧹 代码清理规则

### 使用 swift-format 格式化代码

**命令：**
```bash
swift-format format --in-place --recursive .
```

**参数说明：**
- `format` - 格式化命令
- `--in-place` - 直接修改文件（不输出到新文件）
- `--recursive` - 递归处理所有子目录
- `.` - 当前目录

**使用场景：**
- 提交代码前统一格式
- 清理他人贡献的代码
- 保持代码风格一致

**执行位置：**
```bash
cd ~/Projects/openclaw-deck-swift/openclaw-deck-swift/openclaw-deck-swift
swift-format format --in-place --recursive .
```

**注意事项：**
- 格式化后需要检查是否有误改
- 建议格式化后运行编译测试
- 格式化后应该提交代码

---

## ✅ 代码修改后的标准流程

**每次修改代码后，按以下步骤操作：**

### 1. 自动格式化
```bash
cd ~/Projects/openclaw-deck-swift/openclaw-deck-swift/openclaw-deck-swift
swift-format format --in-place --recursive .
```

### 2. 提交代码
- 格式化后提交
- 提交信息清晰描述修改内容

**好处：**
- ✅ 代码风格统一
- ✅ 保持代码整洁
- ✅ 提交前自动清理

**注意：**
- ⚠️ 不自动执行编译（太耗时）
- ⚠️ 如需验证，手动运行编译脚本
- ⚠️ 格式化可能引入语法错误，需注意

---

## 🔒 编译锁规则（多 AI 协作）

**问题：** 多个 AI 同时编译会导致 `build.db` 数据库锁定冲突

**解决方案：** 构建脚本已自动添加等待逻辑

### 编译前检查

脚本会自动检查 `build.db` 是否被锁定：
- 如果锁定：等待最多 60 秒（可配置）
- 等待期间：每 2 秒显示进度
- 超时后：退出并提示手动清理

### 自定义等待时间

```bash
# 设置更长等待时间（秒）
export MAX_BUILD_WAIT=120

# 运行构建
bash script/build_macos.sh
```

### 如果超时

```bash
# 手动清理构建缓存
rm -rf build/macos/DerivedData
rm -rf build/ipados/DerivedData

# 或者关闭所有 Xcode 进程
killall Xcode
```

### AI 协作建议

1. **编译前询问** - "现在可以编译吗？"
2. **避免同时编译** - 等另一个 AI 完成
3. **使用不同构建目录** - 如需频繁并发编译

---

## 🔒 Git 提交规则

**⚠️ 绝对禁止：AI 不能自行提交代码！**

### 提交流程

```
1. 完成任务 → 2. 列出修改的文件 → 3. 请求用户批准提交 → 4. 用户允许 → 5. 执行提交
```

### 具体要求

1. **禁止自动提交** ⚠️
   - 完成任务后，**不能**直接执行 `git commit`
   - 必须先向用户展示修改内容
   - 等待用户明确说"可以提交"、"提交代码"等

2. **提交范围限制** 📁
   - **只能**提交 AI 自己修改过的文件
   - **不能**提交其他文件（包括构建产物、临时文件等）
   - **不能**提交 `.gitignore` 中忽略的文件

3. **提交信息要求** 📝
   - 必须编写清晰有意义的 commit message
   - 格式：`[类型] 简要描述`
   - 示例：
     - `[feature] 添加会话处理中状态指示器`
     - `[fix] 修复 lifecycle 事件监听逻辑`
     - `[ui] 使用 tint 方法实现玻璃按钮颜色变化`

4. **提交前检查清单** ✅
   - [ ] 只修改了自己改过的文件
   - [ ] 没有包含构建产物（`.build/`, `DerivedData/` 等）
   - [ ] 没有包含临时文件（`.DS_Store`, `*.log` 等）
   - [ ] 提交信息清晰描述了修改内容
   - [ ] **已获得用户明确批准**

### 示例对话

#### ❌ 错误做法
```
助手：任务完成了，我已经提交代码了。
```

#### ✅ 正确做法
```
助手：任务完成了！

**修改的文件：**
- `SessionState.swift` - 添加 isProcessing 状态标志
- `DeckViewModel.swift` - 在 lifecycle 事件中更新状态
- `SessionColumnView.swift` - 使用 tint 实现按钮颜色变化

**建议的提交信息：**
`[feature] 添加会话处理中状态指示器（橘黄色按钮）`

可以提交这些代码吗？

用户：可以提交

助手：好的，正在提交代码...
✅ 提交完成：[feature] 添加会话处理中状态指示器（橘黄色按钮）
```

### 提交命令参考

```bash
# 查看修改的文件
git status

# 添加指定文件
git add <文件路径>

# 提交
git commit -m "[类型] 描述"

# 推送（如果需要）
git push
```

---

**创建日期：** 2026-02-26  
**最后更新：** 2026-02-27（添加文档编写规则）
